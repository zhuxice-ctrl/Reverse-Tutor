{
  "name": "reverse-tutor-mobile",
  "version": "0.17.10",
  "private": true,
  "description": "Reverse Tutor - local-only Android wrapper via Capacitor",
  "scripts": {
    "sync": "node sync-web.js && cap sync android",
    "open:android": "cap open android",
    "build:apk": "node sync-web.js && cap sync android && cd android && gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/preferences": "^6.0.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
